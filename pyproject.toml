[project]
name = "subscription-convert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
    "pyyaml>=6.0.3",
    "requests>=2.34.2",
]
